# 一、常见用法

- 判断返回的值是否相等，自定义值、bool值

- 验证程序的异常

  ```python
  import unittest
  # 1、函数的名称必须以test开头，这是一个约定
  # 2、常用的几个函数：判断值是否相等、判断值是否是bool值
  # 3、使用一些自带的assert方法的目的是，以便后续形成报告
  
  
  def func():
    return 'foo'
  
  
  class TestStringMethods(unittest.TestCase):
  
    # def test_upper(self):
    #   # 检查预期输出
    #   # 检查是否符合预期
    #   self.assertEqual(func().upper(), 'FOO')
  
    # def test_isupper(self):
    #   # 判断返回值是否为bool值
    #   self.assertTrue('FOO'.isupper())
    #   self.assertFalse('FOO'.isupper())
    	
      # 验证是否会发生异常
      # 发生了异常则代表通过
      def test_split(self):
        s = 'hello world 12312'
  
        # 验证是否发生了TypeError
        with self.assertRaises(TypeError):
          s.split(2) # 不允许使用int分割字符串
  
  
  
  if __name__ == '__main__':
    unittest.main()
  
    
    
    
  # 第二种测试异常的方法
  # # _*_ coding:utf-8 _*_
  
  # __author__ = '苦叶子'
  
  # import unittest
  # import sys
  
  # # 除法函数
  # def div(a, b):
  #   return a / b
  
  # # 测试用例
  # class demoRaiseTest(unittest.TestCase):
  
  #   def test_raise(self):
  #     # 程序发生了Zero..异常测试才可以通过，否则不可以通过
  #     self.assertRaises(ZeroDivisionError, div, 1, 0)
  
  # if __name__ == '__main__':
  #   unittest.main()
  
  
  
  ```

  